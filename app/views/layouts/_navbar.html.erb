<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-md">
    <%= link_to "Tela Inicial", root_path, class:"navbar-brand" %>
    <% if user_signed_in? %>
      <div class="btn-group">
      <button type="button" class="btn btn-dark dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
      <%= current_user.email %>
        <ul class="dropdown-menu">
          <%= link_to "Editar Perfil", edit_user_registration_path, class: "btn btn-dark" %>
        </ul>
      </button>
      <%= button_to "Sair", destroy_user_session_path, method: :delete, class: "btn btn-dark"%>
      <% else %>
      <%= link_to "Entrar", new_user_session_path, class: "btn btn-dark "  %>
      <ul class="dropdown-menu">
        <li><%= link_to "Criar conta", new_user_registration_path, class: "btn btn-primary"  %></li>
        <li><%= link_to "Entrar", new_user_session_path, class: "navbar-link"  %></li>
      </button>
      </ul>
      <%end%>
    
  </div>
  </div>
</nav>